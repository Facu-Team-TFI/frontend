import React from "react";

const NotificationCard = ({ notification, removeNotification }) => {
  return <div>NotificationCard</div>;
  // la card sólo tiene que conducir al chat cuando se haga click en ella (y cerrarse) y debe tener también
  // tener un boton para eliminar la notificación
};

export default NotificationCard;
